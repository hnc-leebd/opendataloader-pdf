# AI Issue Label Definitions
# Used by setup-labels.sh to create/update GitHub labels

# Action Labels (ai-issue skill managed)
action:
  - name: fix/auto-eligible
    color: "0E8A16"
    description: "AI auto-fix eligible"
  - name: fix/manual-required
    color: "FBCA04"
    description: "Requires human implementation"

# GitHub Default Labels (not managed by setup-labels.sh)
# Use these standard labels as needed:
# - bug, enhancement, documentation (type)
# - duplicate (triage result)
# - question (needs more info)
# - wontfix (out of scope or rejected)
# - good first issue, help wanted (contribution)
